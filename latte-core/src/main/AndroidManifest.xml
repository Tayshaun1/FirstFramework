<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.latte">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
        allowBackup:其属性值默认是true。当allowBackup标志为true时，用户即可通过adb backup和adb restore来进行对应用数据的备份和恢复，这可能会带来一定的安全风险。
        supportsRtl:声明你的application是否愿意支持从右到左(原来RTL就是right-to-left 的缩写...)的布局。
                    如果设置为true，由于布局方向可以是从右到左的，所以在写xml布局的时候，为了防止出现布局混乱的现象，不要使用诸如layout_marginRight这种，而应该是layout_marginEnd这种。
                    Android4.2以后才有的，并且这个属性默认是false，api 17以前的api不支持这个属性。
    -->
    <!--<application-->
        <!--android:allowBackup="true"-->
        <!--android:label="Latte"-->
        <!--android:supportsRtl="true" />-->

</manifest>
